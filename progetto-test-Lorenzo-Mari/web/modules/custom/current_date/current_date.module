<?php

/**
 * Implements hook_theme().
 */

function current_date_theme($existing, $type, $theme, $path) {
    return [
        'hello-world-template' => [
            'variables' => ['test_var' => 'test completato'],
        ],
    ];
}